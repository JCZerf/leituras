import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color background = Color(0xFFFFFFFF);
  static const Color primaryText = Color(0xFF000000);
  static const Color secondaryText = Color(0xFF303030);
  static const Color border = Color(0xFF000000);
  static const Color surfacePressed = Color(0xFFEDEDED);
  static const Color primaryAction = Color(0xFF000000);
  static const Color success = Color(0xFF000000);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFD32F2F);
  static const Color appBarBackground = Color(0xFF000000);
  static const Color appBarForeground = Color(0xFFFFFFFF);

  // High-contrast palette for direct sunlight and outdoor mobile use.
  static const bool avoidPastelsAndSoftGradients = true;
}
