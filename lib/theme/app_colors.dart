import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color background = Color(0xFFFFFFFF);
  static const Color primaryText = Color(0xFF000000);
  static const Color secondaryText = Color(0xFF4A4A4A);
  static const Color primaryAction = Color(0xFF2E7D32);
  static const Color success = Color(0xFF1E7E34);
  static const Color error = Color(0xFFD32F2F);
  static const Color lightGreenAppBar = Color(0xFF4CAF50);

  // High-contrast palette for direct sunlight and outdoor mobile use.
  static const bool avoidPastelsAndSoftGradients = true;
}
